import React from "react";
import { Center, Text } from "native-base"

const Players = (props) => {
  console.log(props)
  return (
  <Center flex={1}>
    <Text>Playes</Text>
  </Center>)
};

export default Players;
